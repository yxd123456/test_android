package com.hz.greendao.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.hz.greendao.dao.TransformerType;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TRANSFORMER_TYPE".
*/
public class TransformerTypeDao extends AbstractDao<TransformerType, Long> {

    public static final String TABLENAME = "TRANSFORMER_TYPE";

    /**
     * Properties of entity TransformerType.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property Capacity = new Property(1, String.class, "capacity", false, "CAPACITY");
        public final static Property Scbz = new Property(2, String.class, "scbz", false, "SCBZ");
        public final static Property Cjsj = new Property(3, java.util.Date.class, "cjsj", false, "CJSJ");
        public final static Property UpdateDate = new Property(4, java.util.Date.class, "updateDate", false, "UPDATE_DATE");
    };


    public TransformerTypeDao(DaoConfig config) {
        super(config);
    }
    
    public TransformerTypeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TRANSFORMER_TYPE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: id
                "\"CAPACITY\" TEXT," + // 1: capacity
                "\"SCBZ\" TEXT," + // 2: scbz
                "\"CJSJ\" INTEGER," + // 3: cjsj
                "\"UPDATE_DATE\" INTEGER);"); // 4: updateDate
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_TRANSFORMER_TYPE__id ON TRANSFORMER_TYPE" +
                " (\"_id\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TRANSFORMER_TYPE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TransformerType entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String capacity = entity.getCapacity();
        if (capacity != null) {
            stmt.bindString(2, capacity);
        }
 
        String scbz = entity.getScbz();
        if (scbz != null) {
            stmt.bindString(3, scbz);
        }
 
        java.util.Date cjsj = entity.getCjsj();
        if (cjsj != null) {
            stmt.bindLong(4, cjsj.getTime());
        }
 
        java.util.Date updateDate = entity.getUpdateDate();
        if (updateDate != null) {
            stmt.bindLong(5, updateDate.getTime());
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TransformerType readEntity(Cursor cursor, int offset) {
        TransformerType entity = new TransformerType( //
            cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // capacity
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // scbz
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // cjsj
            cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)) // updateDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TransformerType entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setCapacity(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setScbz(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCjsj(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setUpdateDate(cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TransformerType entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TransformerType entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
